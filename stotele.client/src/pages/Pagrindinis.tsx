import React from "react";
import Navbar from "../components/navbar"; // Make sure this matches the export name

const Pagrindinis = () => {
  return (
    <div>
      <h1>Pagrindinis</h1>
    </div>
  );
};

export default Pagrindinis;
